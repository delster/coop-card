import React from "react"
import Deck from "./Deck"

import './Game.sass';

const Game = () => {
  return (
    <main id="main-content" className="section">
      <Deck />
    </main>
  )
};

export default Game;
